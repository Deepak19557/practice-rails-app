<%= form_with(model: lock) do |form| %>
  <% if lock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lock.errors.count, "error") %> prohibited this lock from being saved:</h2>

      <ul>
        <% lock.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lock_name %>
    <%= form.text_field :lock_name %>
  </div>

  <div class="field">
    <%= form.label :key_id %>
    <%= form.text_field :key_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
