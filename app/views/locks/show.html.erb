<p id="notice"><%= notice %></p>

<p>
  <strong>Lock name:</strong>
  <%= @lock.lock_name %>
</p>

<p>
  <strong>Key:</strong>
  <%= @lock.key_id %>
</p>

<%= link_to 'Edit', edit_lock_path(@lock) %> |
<%= link_to 'Back', locks_path %>
