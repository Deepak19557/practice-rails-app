<%= form_with(model: key) do |form| %>
  <% if key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(key.errors.count, "error") %> prohibited this key from being saved:</h2>

      <ul>
        <% key.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :key_name %>
    <%= form.text_field :key_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
